import React from 'react';
import PageTemplate from '../template/PageTemplate';

const MyPage = (props) => (
    <PageTemplate>
        <div>mypage</div>
    </PageTemplate>
);

export default MyPage;
