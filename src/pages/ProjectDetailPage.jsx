import React from 'react';
import PageTemplate from '../template/PageTemplate';

const ProjectDetailPage = (props) => (
    <PageTemplate>ProjectDetailPage</PageTemplate>
);

export default ProjectDetailPage;
